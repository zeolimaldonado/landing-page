---
const { title, description, image } = Astro.props
---
<div class="flex flex-col gap-6 justify-start items-center px-4 py-6">
    <div class="w-full aspect-video rounded-3xl bg-gray-400 overflow-hidden">
        <img src={image} alt={title} class='w-full object-scale-down' />
    </div>
    <div class="space-y-3">
        <h5 class="text-2xl font-semibold text-slate-400 text-center">
            {title}
        </h5>
        <p class="text-slate-300 font-semibold text-center max-w-[65ch] mx-auto text-balance">
            {description}
        </p>
    </div>
</div>