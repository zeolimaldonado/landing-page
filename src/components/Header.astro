---
import ButtonDemo from "./ButtonDemo.astro"
import NavigationItem from "./NavigationItem.astro"

import { navigation } from "../constants";
---

<section class='w-screen h-16 py-2 bg-neutral-900 bg-opacity-30 fixed top-0 z-50'>
	<nav class="h-full container mx-auto flex justify-between gap-2 items-center">
		<div class="flex-1">
			<h4 class="text-white">
				CTAPP
			</h4>
		</div>
		<ul class="flex justify-center items-center gap-4">
			{
				navigation.map((item) => (
					<NavigationItem href={item.href}>
						{item.title}
					</NavigationItem>
				))
			}
		</ul>
		<div class="h-full flex-1 flex justify-end items-center">
				<ButtonDemo />
		</div>
	</nav>
</section>